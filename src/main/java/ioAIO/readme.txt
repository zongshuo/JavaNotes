AIO：异步非阻塞IO，当进程发起一个io操作，进程返回（不阻塞），但也不能返回结果；
     内核把这个io处理完后，会通知（回调）进程结果。如果io操作成功则进程直接获取到数据。
     特点：不阻塞，数据一步到位；
          Proactor(主动器)模式；
          需要操作系统底层支持，Linux2.5版本首现，Linux2.6版本标准特性；
          实现、开发应用难度大；
          非常适合高性能高并发应用。
          //TODO 代码实现？